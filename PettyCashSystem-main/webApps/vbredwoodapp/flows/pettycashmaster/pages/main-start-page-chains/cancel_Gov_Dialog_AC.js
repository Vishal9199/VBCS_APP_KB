define([
  'vb/action/actionChain',
  'vb/action/actions',
  'vb/action/actionUtils',
], (
  ActionChain,
  Actions,
  ActionUtils
) => {
  'use strict';

  class cancel_Gov_Dialog_AC extends ActionChain {

    /**
     * @param {Object} context
     * @param {Object} params
     * @param {object} params.event
     * @param {any} params.originalEvent
     */
    async run(context, { event, originalEvent }) {
      const { $page, $flow, $application, $constants, $variables } = context;

      const governorateDialogClose = await Actions.callComponentMethod(context, {
        selector: '#governorateDialog',
        method: 'close',
      });
    }
  }

  return cancel_Gov_Dialog_AC;
});
